import { bootstrap } from "../flux/bootstrap.ts";
import { fluxParticle } from "../flux/fluxParticle.ts";

bootstrap({
  particles: [
    // debugParticle,
    // configParticle,
    // consoleParticle,
    fluxParticle,
    // versionParticle,
  ],
});
